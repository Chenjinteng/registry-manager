# 更新日志

版本号规则见 [AGENTS.md](./AGENTS.md#版本号规则)：`主.中.小` 三位。

- **主**：由人决定，新增/破坏性变化时才动；
- **中**：每新增一个功能或模块 +1；
- **小**：缺陷修复与现有功能优化。

## [0.8.1] - 2026-09-24

修一个"改了热度却看不见"的问题。按版本规则是**小版本**（缺陷修复）。

起因是用户反馈：用本工具的「镜像拉取」搬了一个镜像，**热度里没看到**。
查下来是两件事，一件是 bug、一件是理解偏差：

### 修复

- **自身请求被整类折叠，把"计入热度的那些"也一起挡掉了。**
  0.7.1 为了不让一次盘点（上百条读请求）冲掉排查面板，把 `registry-manager/*` 的事件
  全部折叠成一个数字。但这一类里混了两种性质完全不同的东西：

  | 自身请求 | 是否计入热度 | 一次几条 | 该不该进面板 |
  | --- | --- | --- | --- |
  | 盘点读 manifest / config blob | ❌ 永远不计入 | 上百条 | ❌ 折叠（0.7.1 的初衷） |
  | **拉取任务落目的端 manifest（PUT）** | ✅ **计入** | 每个 tag 1 条 | ✅ **必须留着** |

  挡掉后者的后果很具体：**热度 +1 了，但「最近事件」里一条记录都没有**，
  于是"热度为什么变了"根本查不出来 —— 而这正是这个面板存在的理由。
  折叠的判据改成 `self && !counted`（**不计入**的自身请求），不是 `self`。

### 变更

- 面板标题上的「自身请求 N 条」改叫「**自身读取** N 条」，语义收窄为"被折叠的那些"。
  计入热度的自身请求现在正常列在表里（UA 是 `registry-manager/<版本>`）。

### 文档

- README 的「怎么算"一次拉取"」补一条容易误解的账：
  **用本工具的「镜像拉取」把镜像搬进这个 registry，在这里算一次 `push`（不是 pull）**
  —— 它确实往本仓库写了一个 manifest，而"拉取次数"指的是别人从这个 registry 拉走。
  所以要去「推送次数」KPI 或 Top 榜单的「推送」列看，别在「拉取次数」里找。
- AGENTS.md：把折叠判据写清楚（`self && !counted`），并记下这次踩的坑。

## [0.8.0] - 2026-09-24

忽略规则改成**在界面上管理**。按版本规则，新增一项用户可感知的能力 → 中版本 +1。

起因是个很具体的摩擦：0.7.0/0.7.2 的规则只能写环境变量，改一条要走
「编辑 `.env` → 重启容器」—— 而你要排掉某个客户端时，人正站在热度页「最近事件」
前面看着那条 UA，这中间隔了一整个部署流程。

### 新增

- **热度页每一行的「客户端」格子上有「忽略」入口**。点开是一个弹框：**预填规则片段**
  （取 UA 的第一段，`regclient/regsync`），并**实时显示"会匹配到最近收到的哪几个客户端"**。
  预览是这个弹框存在的主要理由 —— 规则是子串匹配，写宽一点就会误伤，
  得让人在按确定之前先看见后果。
  刻意**不预填完整 UA**：`regclient/regsync (v0.11.5)` 存进去，等对方升到 v0.12
  规则就失效了，而且失效时没有任何提示。
- **规则存 SQLite（`ignored_clients` 表），增删立即生效，不用重启**。
  不写回 `registry.config.json`：那是人写的配置文件，工具偷偷改它迟早冲突；
  而且容器里它常常是只读挂载甚至压根没挂进去，只有 `/app/data` 是可写卷。
- **设置页新增「热度忽略规则」面板**：看全量、删规则、**标出每条规则的来源**。
- **接口**：`GET/POST/DELETE /api/stats/ignore`。

### 变更

- **规则有两个来源，取并集**：环境变量 `REGISTRY_STATS_IGNORE_USERAGENTS` 作为
  **声明式部署的基线**（界面上只读、删不掉），界面是日常入口。
  分开显示来源是刻意的 —— 混成一条列表的话，"删了没反应"的用户根本不知道去哪找。
  服务端也真的删不动环境变量那条（不只是界面上把按钮藏起来）。
- 环境变量那条**降级为可选**，不再写进 `.env.example` 的主配置块：
  日常请用界面。

### 修复 / 加固

- **空规则会归零全部热度**：`''.includes` 恒为真，一条空规则会把所有事件判成忽略。
  接口层拒收，匹配函数里也显式跳过空/空白片段（两道，不指望调用方都校验）。
- `activities` 的去重与删除按**小写**比对：`RegClient/RegSync` 与 `regclient/regsync`
  不会存成两条、也不会删了一条还剩一条。

### 文档

- README「怎么排」改成从界面走；`.env.example` 的规则变量降级为"可选（声明式部署）"。
- AGENTS.md：补上"规则两个来源、取并集、环境变量那条删不掉"的设计约束，
  以及 `ignored_clients` 表的迁移要求。
- `verify:pull-history` 的迁移断言从 v1→v2 扩到 **v1→v3 跨两步**与 **v2→v3**，
  都要求原有的热度一行不丢。

## [0.7.2] - 2026-09-24

补一个 0.7.1 漏掉的地方。按版本规则是**小版本**（缺陷修复）。

问题是被一个提问引出来的："registry-manager 这个 ua 是直接过滤了吗？"
回头审计**所有**出站路径才发现：0.7.1 只给走 `#request` 的常规请求加了 User-Agent，
而 `registry-client.mjs` 里还有两条**直接用 `undiciFetch`** 的写路径绕过了它。

### 修复

- **拉取写目的端的两条路径没带 User-Agent**：
  - 上传会话的 `PATCH`（`streamBlobToDest`，流式转发 blob）
  - 目的端落 manifest 的 `PUT`（`putDestManifest`）

  所以只要用一次「镜像拉取」，registry 侧就会又冒出认不出来的 `undici`，
  而且它们**不会被算进「自身请求」计数**（UA 不匹配前缀）。
  两条都补上 `USER_AGENT`。
- 验证脚本里的 UA 断言一并从"只跑读路径"扩到**三条路径**
  （`GET /v2/_catalog` / `PATCH .../blobs/uploads/...` / `PUT .../manifests/...`）——
  之前那条断言之所以没发现，就是因为它只覆盖了 `#request`。
  回退验证：补丁前后各去掉一条，断言分别报 `PATCH undici` 与 `PUT undici`。

## [0.7.1] - 2026-09-24

修正"热度面板里 178 条 `undici` 是什么"。按版本规则是**小版本**（缺陷修复 + 既有功能优化）。

那 178 条**是本工具自己**：`undici` 是 Node 内置的 HTTP 客户端，我们从来没设过 User-Agent。
数字也精确对得上 —— 76 仓库 / 88 tag 的一次「重新扫描」= 每个 tag 一次 manifest GET
+ 一次 image config blob GET ≈ 178 条事件，正好把 200 条的排查缓冲冲干净。

### 修复

- **本工具自己的请求带上自己的 User-Agent**：`registry-manager/<版本>`（之前是裸的 `undici`）。
  现在从 registry 侧的事件里一眼能认出哪条是我们发的，不会再出现"这个 undici 是什么"。
- **自身请求不再进排查缓冲**，只单独计数、在面板标题上显示「自身请求 N 条」。
  不这样做的话，一次盘点就能把缓冲冲成自己的噪音，真正要查的东西（例如同步工具的 UA）
  只剩 22 条 —— 等于把这个面板废掉了。信息没被藏起来，只是不再淹没信号。
  它们也**不计入面板的「计入 / 未计入」**：那两个数要和下面那张表对账，
  自身请求既然不在表里，就不该出现在那两个数里。

### 变更

- **计数语义一个字没改**：自身请求照常参与口径判定。盘点读的是 GET manifest 与 blob，
  本来就不计入；**拉取任务往本仓库写 manifest（PUT）仍然计入热度** ——
  那是真实发生过的 push。想让它们也不算，把 `registry-manager` 加进
  `REGISTRY_STATS_IGNORE_USERAGENTS` 即可（UA 现在认得出来了，才有这个选项）。

### 文档

- AGENTS.md / README：补上"看到 `undici` 就是本工具自己"这条，以及自身请求不进缓冲的规则；
  顺带把 `request.addr` 那段过时的绝对结论（"端口映射下一定是网桥网关"）改成与实测一致的说法。

## [0.7.0] - 2026-09-24

上一轮把"是谁在打"看清了（0.6.0），给出真实事件的 User-Agent 之后，这一轮把**排除规则**做出来。
按版本规则，新增一项用户可感知的能力 → 中版本 +1。

### 新增

- **排除指定客户端的热度事件**：`REGISTRY_STATS_IGNORE_USERAGENTS`（逗号分隔、**子串匹配、
  忽略大小写**）。用来排掉 registry 上常驻的同步工具 —— 它们按点扫全量，会把每个 tag 的
  热度刷成同一个数，于是热度榜测的是工具的心跳而不是人。

  ```bash
  REGISTRY_STATS_IGNORE_USERAGENTS=regclient/regsync,skopeo
  ```

  用子串而不是全等，是因为实测到的 UA 带版本号（`regclient/regsync (v0.11.5)`），
  写片段就能匹配，对方升版本不用改配置。
- **被排除的事件仍然留在「最近事件」里**，`reason` 标成 `IGNORED_USERAGENT:<命中的片段>`，
  `counted=false`。这是刻意的：不这样做的话，"被排掉了所以热度不涨"和"事件根本没到"
  在界面上长得一模一样，下次排查又要绕一圈。
- **配置可见**：忽略规则会挂在「最近事件」面板标题上（「已忽略：…」标签），
  也随 `/api/config` 下发，启动日志里也会打一行 —— 配了没生效 vs 配了生效，必须能分辨。
- 规则命中**放在口径判定最前面**：整类客户端都不算的时候，它的 blob 事件、GET 事件也都不算，
  reason 直接说"这个客户端被忽略了"比特意去区分"它是 blob 还是 GET"更有用
  （后者会让人以为只忽略了那一部分，进而怀疑配置没生效）。
- 界面上一句人话：数据里的 `IGNORED_USERAGENT:regclient/regsync` 显示成
  「已忽略客户端 regclient/regsync」（完整 reason 仍保留在接口与缓冲里，排查时要 grep 它）。

### 修复

- **长 reason 把「最近事件」表撑出横向滚动**：`IGNORED_USERAGENT:regclient/regsync` 这种
  没有空格的长 token，默认断行规则不会在中间折行，溢出单元格、撑出 80px，把最右边的列推出视野。
  根因是「是否计入」那格用了 `<Space>`（inline-flex + `flex-wrap: nowrap`，既不折行也不收缩），
  改成普通行内流 + `.reason-text { overflow-wrap: anywhere }`。
  这是加了排除规则之后才暴露的（reason 变长了），`pnpm verify:layout` 的横向滚动断言当场抓到
  （1374 → 1454），修完回到 `scrollWidth === clientWidth === 1374`。

### 修正

- **`request.addr` 的结论写得太绝对了。** 之前 AGENTS.md / README 里写的是"端口映射下
  一定是 Docker 网桥网关，不能用来识别客户端"。实测真实事件后修正为**看场景**：
  - 客户端在**别的机器**上 → 是**真实客户端 IP**（实测 `192.0.2.11:50672`），**可以**用；
  - 客户端与 registry **同宿主**（含容器端口映射）→ 才是网桥网关（如 `172.19.0.1`），
    那时对所有人都是同一个值。
  所以判断"能不能按 IP 排"之前，先看一眼真实事件里的 `addr` 分布；UA 才是无条件可靠的那个。

### 文档

- README 新增「自动化流量会把热度刷高 § 怎么排」：看清是谁 → 填配置 → 清掉历史，
  三步带可复制的命令。
- AGENTS.md：身份字段对照表按实测结论修正、补上排除规则的实现约束（三处透传，
  任何一环断了表现都是"配了规则但热度照旧被刷"）。

## [0.6.1] - 2026-09-24

修复 0.6.0 引入的一处布局回归。按版本规则是**小版本**（缺陷修复）。

### 修复

- **「最近事件」面板加了「客户端」列之后整张表横向滚动**，最右边的「是否计入」
  （写着未计入的原因，是排查的一半信息）被推出视野 —— 等于"加了信息反而更难用"。

  根因不是"列太多"，而是 **AntD 表格默认是 auto 布局**：长且不可折行的内容会把列撑到
  自身宽度，列上的 `width` 变成摆设。实测 `mediaType` 声明 240 实际撑到 454、
  新的「客户端」列撑到 943，加起来 1952 > 容器 1374。

  修法是在**列上**声明 `ellipsis: { showTitle: false }` —— AntD 接到它才会把表格切成
  fixed 布局；只在单元格里套一个 `.ellipsis` 类是没用的。`showTitle: false` 是因为
  这两列已经有内容更全的自定义 Tooltip，不要再来一个原生的。
  修完实测 `scrollWidth === clientWidth === 1374`，各列按声明生效
  （mediaType 210 / 客户端 210 / 仓库 224），「收到时间」也不再被压成四行。
- `pnpm verify:layout` 增加一条断言把这件事钉住：**面板不需要横向滚动**
  （`scrollWidth <= clientWidth`）。这条只有真去量 DOM 才知道，读代码和 type-check
  都发现不了 —— 它是这次改动的**自查**发现的（改完先量了一遍），不是上线后被反馈的。

## [0.6.0] - 2026-09-24

本轮新增**清空热度数据**，并让「最近事件」面板带上客户端身份。
按版本规则，新增一项用户可感知的能力 → 中版本 +1。

起因是一个真实问题：某台 registry 上常驻 regsync 在做实时同步，**按点扫全量**，
于是每个 tag 的热度都被刷成同一个数、「最近活动」也全是同一个时刻 —— 热度榜测的是
工具的心跳，不是人。而**热度口径本身没错**（那些确实是 push/pull 事件），
缺的是"把机器和真人分开"的能力。

### 新增

- **清空热度数据（从头重计）**：设置页新增入口，一键清空并按新口径从头累计。
  只清按天聚合与幂等去重记录，**拉取历史不受影响**；返回删掉的行数并在界面回显，
  不只说一句"成功"。
  存在的理由：口径改正**只对以后生效** —— 已经聚合进 `activity_daily` 的行当初没留
  身份字段，追溯不出来，只能清掉重新累计。代价是清空瞬间若 registry 正在重投旧事件，
  会有少量被重新计入，这对"从头重计"这个语义是合理的。
- **`DELETE /api/stats/heat`**：上面那个动作的接口。
- **「最近事件」面板新增「客户端」列**：显示事件的 `User-Agent`，悬停可看来源地址、
  Host 与账号。这是排查"热度是不是被机器刷高了"唯一可靠的一列。
- **原始事件缓冲补四个身份字段**：`request.useragent` / `request.addr` /
  `request.host` / `actor.name`，随 `/api/stats/events` 一并下发。
  四个字段都做**长度截断** —— 它们来自外部输入，不能无界撑大内存里的排查缓冲。

### 变更

- 热度口径**没有变**（这一轮不加任何过滤）。先把"谁在打"看清楚，再决定过滤规则，
  避免又一次"跟着猜测写实现"。

### 文档

- README 新增「自动化流量会把热度刷高」一节：为什么 registry 侧排不掉它
  （`notifications` 只有 `ignore.mediatypes` / `ignore.actions`，没有按客户端过滤的入口），
  以及四个身份字段各自能不能用来区分客户端 —— **只有 `request.useragent` 可靠**。
- README 接口表补 `DELETE /api/stats/heat`；修正原先"不统计客户端 IP"的说法
  （现在会为了排查而记录，但它仍然不能用来识别客户端）。

## [0.5.0] - 2026-09-24

本轮新增**深色主题**。按版本规则，新增一项用户可感知的能力 → 中版本 +1
（同轮还有一处样式缺陷修复，取最高档，不累加）。

### 新增

- **深色主题**：顶栏右上角一键切换，**选择被记住**（`localStorage`），
  首次打开跟随系统偏好（`prefers-color-scheme`）。刷新**不会闪白** ——
  主题在 `<head>` 里的内联脚本中、React 挂载之前就设好；交给 React 的 `useEffect`
  去设等于第一帧先按浅色渲染，深色用户会看到一瞬白屏。
- **`scripts/verify-theme.mjs`**（`pnpm verify:theme`）：静态钉住四类"不报错但长得不对"的
  主题缺陷 —— ① `:root` 的 token 有没有在深色块里漏覆盖（漏掉的那个继续用浅色值，
  表现为"某个角落突然是白的"）；② 代码里引用的自定义属性是否真的存在；
  ③ `main.tsx` 的 AntD token 与 `theme.css` 的语义色是否**是同一组值**（否则同屏两种蓝）；
  ④ 首屏内联脚本是否仍在模块脚本之前。四类都做过**回退验证**，确认断言挡得住回归。

### 变更

- **配色分两套，靠 `<html data-theme>` 切换**：`web/src/theme.css` 的 `:root`（浅色）
  与 `[data-theme='dark']`（深色）必须一一对应，两套都设了 `color-scheme`，
  否则深色页面上会插着一根亮色的原生滚动条。
- **AntD 跟着一起变**：`main.tsx` 在深色下用 AntD 的 `darkAlgorithm` 打底
  （它负责上百个我们没显式列出的派生色：禁用态、hover、阴影），再把语义色对齐到
  `theme.css` 的深色值。只改自己那套 `--color-*` 会得到**"半深色"**——
  自定义 CSS 变深了，AntD 的表格 / 分页器 / 标签还是浅色，同一屏两套配色。
  这是深色主题最常见的翻车方式，已由 `pnpm verify:layout` 真的读 computed style 挡住
  （回退验证：去掉 `darkAlgorithm` 后 `.ant-tag` / `.ant-table-thead th` 仍是浅色，断言失败）。
- **深色不是把浅色取反**：深色底上的对比度感受不同 —— 主色提亮
  （`#155aef` → `#4c8dff`，原来的蓝压在深色底上发闷）、文字改用低饱和冷灰、
  层级靠"比背景亮一点"而不是深色描边。

### 修复

- **失败状态的高亮一直是失效的**：`web/src/pages/pull-page.tsx` 用的是
  `var(--color-error)` / `var(--color-error-bg)`，而这两个 token **从未存在过**
  （主题里叫 `--color-fail`）。CSS 自定义属性没有值域检查 —— 名字写错不会报错、
  不会继承、直接当未定义，整条声明被静默丢弃。已改为 `var(--color-fail)`，
  并补上缺失的 `--color-fail-bg`（失败态浅色底块）。
  `pnpm verify:theme` 现在会扫描全部 `.css` / `.tsx` / `.ts` 里的 `var(--…)` 引用，
  引用了不存在的 token 直接失败（回退验证：改回 `--color-error` 后断言失败）。

### 文档

- `docs/design.md` §5.1：深浅主题为什么这么实现（两套 token、`darkAlgorithm`、
  内联脚本、`color-scheme`）。
- `AGENTS.md`：视觉约定补主题规则，验证清单补 `pnpm verify:theme`。
- `README.md`：能力列表补深色主题。

## [0.4.0] - 2026-09-24

本轮新增**镜像热度**模块。按版本规则，新增模块 → 中版本 +1。

### 新增

- **镜像热度**（新模块）：统计每个仓库与 tag 被 push / pull 的次数。
  数据来自 Distribution 原生的 `notifications` webhook，**管理服务只收事件、不进数据面**
  （`docker pull` 的字节一个包都不经过它）。按 `(天, 仓库, tag, 动作)` 聚合，默认保留 90 天。
- **热度页**：时间窗切换（7 / 30 / 90 天）、KPI 总览、按仓库与按 tag 的 Top 榜单、
  手写 SVG 的按天趋势，以及一个「最近事件」排查面板。
- **镜像列表新增热度列**：热度与最近活动时间，可排序。热度不可用时整列隐藏。
- **SQLite 存储**：用 Node 22 自带的 `node:sqlite`，**不引入任何依赖、不需要额外的数据库服务**，
  只落一个文件在数据目录里；WAL 模式，去重与计数在同一个事务里完成。
- **镜像拉取历史落库**：任务到达终态时追写一条到 SQLite，**重启不再丢失** ——
  原来任务只在内存里，重启一次就答不了"上周搬了哪些镜像"。
  运行态（排队 / 执行中 + 实时进度）仍留内存；失败 / 取消的任务保留阶段明细，
  成功任务只存汇总（20 层的拉取有 22 条 phase，存了只会把行撑胖）。
  默认保留 90 天，由 `REGISTRY_PULL_HISTORY_RETENTION_DAYS` 调整（与热度的保留期**分开配置**）。
- **`scripts/verify-pull-history.mjs`**（`pnpm verify:pull-history`）：钉住存储契约，
  其中最关键的一条是**老库（只装了热度的 `user_version=1`）升级到 v2 不丢数据**。
- **接收端可观测性**：内存里保留最近 200 条原始事件（**含未被计入的原因**），
  事件没到、口径不对时能直接看出来。
- **按天趋势做成贡献日历**（GitHub 那种方格热力图），而不是折线：折线看不出"周内高峰、
  周末低谷"这类节奏，而这才是热度真正要回答的问题。仍然是手写 SVG，**不引图表库**。
  日历**固定看近 12 个月**（≈53 列）并**按容器宽度反算格宽**，因此铺满整行 ——
  原先跟随时间窗，30 天只有 5 列，不论格子多大都缩在卡片左边、右侧一大片空白。
  没有活动的日期画成灰色方块（和 GitHub 一样）；保留期短于跨度时 caption 会说明
  "更早的灰色是已过期，不代表没有活动"，避免被读成"那段时间没人用"。
- **布局：滚动只发生在表格内部**（镜像列表、热度榜单）。原来整页一起滚 ——
  往下翻表格会把搜索框、时间窗、KPI 一起顶走，而"翻到第 30 行"和"改搜索词"
  本来就是交替发生的动作。表头粘住、分页器钉在卡片底部。
- **`scripts/verify-layout.mjs`**（`pnpm verify:layout`）：用无头 Chrome 真的滚一遍并截图，
  把"渲染结果无法靠读代码确认"变成可执行的检查。
- **`docs/design.md`**：产品设计说明，写清每个页面只该有什么、以及**哪些话刻意不写在界面上**。

### 变更

- **界面文案的规矩：只写"这里能做什么"，不写"为什么这么实现"。**
  凭据管理、代理管理、镜像热度三页的副标题原本混着实现原理（AES-256-GCM、
  `REGISTRY_CREDENTIAL_KEY`、webhook 口径、`manifest HEAD` 等），现全部收成一句话；
  原理移入 `README.md` 与 `docs/design.md`。表单提示里的算法名同样改为"加密存储"。
  两个理由：原理不该给使用者看（否则要先读懂源码才能用工具），
  而且界面上的实现细节不会随代码更新而漂移。
  同时把副标题的 `max-width` 从 720px 放宽到 880px —— 实测「镜像拉取」那句需要 825px，
  卡在 720 会折成两行且末行只剩几个字；并加 `text-wrap: pretty` 防止窄屏下的孤行。
- `engines.node` 从 `>=20` 提到 **`>=22.5.0`**（`node:sqlite` 的要求）。
- 启动脚本加 `--disable-warning=ExperimentalWarning`：`node:sqlite` 在 Node 22 上仍标注实验性，
  这条噪音会盖住真正有用的告警。
- 「数据目录」语义明确化：凭据库、代理库与热度数据库共用一个目录。变量名仍是
  `REGISTRY_CREDENTIALS_DIR`，以免破坏已有部署的挂载与卷。
- 新增配置 `REGISTRY_NOTIFY_TOKEN`（只从环境变量读）、`REGISTRY_ALLOW_REGISTRY_EVENTS`、
  `REGISTRY_STATS_RETENTION_DAYS`、`REGISTRY_PULL_HISTORY_RETENTION_DAYS`。
- `REGISTRY_PULL_QUEUE_SIZE` 的含义收窄为"**内存里**保留的任务条数"；
  完整历史改由 SQLite 承担（见上）。
- **凭据与代理保持原样**（整份 AES-256-GCM 加密的独立 JSON），**刻意不进 SQLite**：
  进表会把 `name` / `registry_url` / `username` 变成明文，是从"整份不可读"退化成
  "元数据全暴露"，而且会让敏感数据与非敏感数据混进同一个备份单元。理由见 `docs/design.md` §4。

### 修复

- **热度榜单切换「按仓库 / 按 tag」时残留旧行**。`rowKey` 原本依赖聚合维度
  （`topBy === 'tag' ? repo:tag : repo`），切换时同一个 record 的 key 会变，
  React 因此无法正确协调 —— 旧行留在 DOM 里：分页器显示"共 8 项"而 DOM 里有 16 行、
  `data-row-key` 重复，界面上表现为 **Tag 列错位 / 空白**，而且每切换一次就多留一批
  （17 → 25 → 33 …）。改成与维度无关的稳定 key（`repo\u0000tag`）。
  已在 `pnpm verify:layout` 里加了浏览器回归断言（连切 6 次，校验行数=分页总数、
  无重复 rowKey、单元格数与表头一致），**回退该修复后断言必然失败**（已实测：
  报出 `domRows:16 dataRows:8 duplicateKeys:8`）。

### 文档

- 新增 `docs/pull-heat.md`：记录**全部来自真实 registry 的实测事实** ——
  一次 pull 会产生哪些事件、为什么带 tag 的是 HEAD 而不是 GET、
  push 为什么会发出假的 pull 事件、Prometheus 指标为什么拿不到仓库维度、
  以及"拿 manifest 必须带 `Accept`，否则回 404"这个坑。
- README 增补「镜像热度」一节（registry 侧配置、口径表、已知边界）。

### 已知边界

- 热度从配置生效那天开始统计，之前的历史补不回来。
- 失败的拉取（401 / 404）不产生事件，热度只反映**成功**的推送与拉取。
- 不做客户端 IP 统计：端口映射后 registry 看到的是 Docker 网桥地址，不是真实客户端。
- 无法区分"按 tag 拉"与"按 digest 拉"——每次普通 tag 拉取都会附带一条按 digest 的 GET 事件。

## [0.3.1] - 2026-09-22

### 新增

- 顶栏左侧显示**当前运行中的版本号**（如 `v0.3.1`）。
  版本由服务端从 `package.json` 读取后经 `/api/config` 下发，
  界面上**不写死**，避免与包版本漂移；读不到时不显示，不影响服务启动。

## [0.3.0] - 2026-09-22

本轮相对 0.2.0 新增了**两个模块**（凭据管理、代理管理）与数项新能力，按规则中版本 +1。

### 新增

- **凭据管理**（新模块）：外部 registry 的 basic auth 凭据库，AES-256-GCM 加密落盘，
  密钥由 `REGISTRY_CREDENTIAL_KEY` 经 scrypt 派生；密码不回显、不进任务历史。
- **代理管理**（新模块）：外部源 HTTP 代理库（地址 + 可选账号密码），
  并支持**连通性测试**（实际穿过代理访问目标，报告状态码与耗时）。
- **Bearer 令牌认证**：支持 Docker Hub / ghcr.io / quay.io / 华为 SWR 等
  以 `WWW-Authenticate: Bearer` 认证的 registry，匿名即可拉公开镜像；
  token 按 scope 缓存复用。此前只支持 basic auth，这类源一律报"要求认证"。
- **创建任务前预览**：列出解析后的全部字段并做只读预检 —— 源是否可达、
  源 tag 是否存在、目标 tag 是新建 / 已一致 / 将被替换（附替换前后 digest）。
- **任务级来源代理**：可选"不用 / 代理库 / 临时输入"，临时输入不落库。
- **任务级源认证**：可选"不用 / 凭据库 / 临时输入账号密码"。
- **拉取失败区分源端 / 目的端**：错误码带 `origin`，提示文案针对性强。
- **目标镜像名可编辑**：本仓库地址以固定文本呈现（不可编辑），后半段可改，
  例如把 `library/alpine:3.19` 落成 `alpine:3.19`。
- **Docker Hub 官方镜像自动补 `library/` 前缀**：`nginx:latest` → `library/nginx:latest`，
  与 `docker pull` 行为一致；`docker.io` / `index.docker.io` 等别名也归一到
  `registry-1.docker.io`。

### 变更

- **认证与代理按作用对象分两处**：本 registry 自身的认证与代理属部署级配置
  （`registry.config.json` / `REGISTRY_USERNAME`、`REGISTRY_PASSWORD`、`REGISTRY_PROXY`），
  因为它们缺失时连镜像列表都打不开；凭据库与代理库只服务**外部源**，
  因此凭据/代理都不再有"用途"维度。
- 拉取表单只保留「源镜像名」一个必填项，目标引用自动补全。
- 任务列表所有行均可展开（成功/进行中也能看阶段详情）；首屏收起，
  之后新出现的失败任务自动展开一次。
- 加密存储的数据目录在启动时**主动探测可写性**，不再等首次写入才失败。
- `REGISTRY_PROXY=` 显式置空现在能覆盖配置文件里的代理值。

### 修复

- **流式 PATCH 缺 `duplex: half`**：请求根本发不出去，每一次真实拉取都失败
  （表现为"目的 PATCH 失败"但服务端没收到请求）。
- **目的端凭据未回写 `destClient`**：mount 与上传全是裸请求。
- **上传会话 `Location` 跨主机时 PUT 被拼成畸形 URL**：表现为
  "manifest 已读取但目的 PUT 404"。
- **`/v2/` 探测把"需要认证"误判为"源不可达"**：预览因此禁用入队按钮，
  而带 scope 的读取其实完全正常（ghcr.io / quay.io 实测触发）。
- **空 scope 的令牌申请被直接放弃**：Docker Hub 的 `/v2/` 挑战不带 scope，
  导致探测永远失败。
- **镜像站的网站被当成 registry**：这类地址返回 nginx 的 `Basic realm`
  且无 registry 版本头，现在报 `NOT_A_REGISTRY` 并说明原因，
  而不是让用户去配一个永远配不对的凭据。
- **任务行展开后收不回去**：AntD `expandedRowKeys` 是受控属性，缺 `onExpandedRowsChange`。
- **数据目录"存在但不可写"时静默成功**：直到新增凭据才失败，且错误与权限无关。
- **凭据库初始化失败被误报成"未配置 KEY"**：把已配好密钥的人引向错误方向。
- **容器内 `/app/data` 未预建**：非 root 用户无法在 `/app` 下创建，镜像已预建并交给 `node`。
- **令牌申请失败被误报成 `CONNECTION_FAILED`**：掩盖真实认证失败。
- **示例地址泄漏真实内网 IP 与组织名**：示例统一改用 RFC 5737 文档地址与 `example.com`。

### 文档

- README 增补：认证/代理分工、凭据与代理管理、Bearer 令牌流程、
  上传 `Location` 跨主机、数据目录排障、创建前预检。
- 新增非破坏性回归脚本（`pnpm verify`）与真实 registry 验证（`pnpm verify:real`）。
- 说明 `REGISTRY_CREDENTIAL_KEY` 与凭据文件需**一起备份**，同时丢失不可恢复。

## [0.2.0] - 2026-09-22

### 新增

- **镜像拉取**：从外部 registry 拉取镜像落到当前仓库。
  单并发 + FIFO 队列；优先走 cross-repo mount，未开启时回落流式 PATCH，
  数据始终在两个 registry 之间流式搬运，不经过进程内存。
- **优雅取消**：标记 `cancelled` 后立即让源/目的流停止传输，
  当前正在写的 chunk 写完才退出，目的端不会留下半截 manifest。
- 任务级来源代理（手填地址）。
- 拉取任务列表：状态、进度、阶段明细，失败原因默认展开。

### 变更

- 新增 `allowPull` / `pullQueueSize` 配置。

## [0.1.0] - 2026-09-22

### 新增

- 初始版本：浏览与管理 CNCF Distribution（Docker Registry HTTP API V2）镜像。
- 镜像列表（搜索、按体积/构建时间排序）、镜像详情（digest、架构、层数、体积、
  构建时间、复制 `docker pull`）。
- 按 digest 删除 manifest，删除前列出同一 digest 下的全部 tag。
- 清单概览与连接自检。
- 连接配置：`REGISTRY_URL` / `REGISTRY_PROXY` / `REGISTRY_ALLOW_DELETE`，
  支持环境变量与 `registry.config.json`（环境变量优先）。
- 三阶段 Dockerfile，非 root 运行。
